<Window x:Class="PL.Catalog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource self}}"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="Black">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="11*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Button Content="" HorizontalAlignment="Left" Height="61" VerticalAlignment="Top" Width="53" Foreground="#FFEFCF81" FontSize="20" Margin="163,363,0,0" FontFamily="Segoe Print" FontWeight="Bold" Grid.Column="1" Click="Button_Click">
            <Button.Background>
                <ImageBrush ImageSource="/צילום מסך 2023-01-13 104936.png"/>
            </Button.Background>
        </Button>
        <ComboBox HorizontalAlignment="Left" Name="SelectedCategory"  Margin="400,0,0,402" Width="99"  SelectedIndex="5" Height="20" VerticalAlignment="Bottom"/>
        <Button Content="Group by Category" HorizontalAlignment="Center"  Height="33" Margin="0,22,0,0" VerticalAlignment="Top" Width="146" Click="Group_Click" Grid.Column="1"/>
        <ListView  x:Name="groupListView"  ItemsSource="{Binding ProducitemtList,Mode =TwoWay}" HorizontalAlignment="Left" Height="200" Margin="30,39,0,0" VerticalAlignment="Top" Width="469"  MouseDoubleClick="MouseDoubleClick1">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>




    </Grid>
</Window>
